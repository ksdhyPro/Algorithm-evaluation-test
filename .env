# ==================== 基础目录配置 ====================
# 项目基础目录，存储所有算法数据
# BASE_DIR=G:\\2
BASE_DIR=./projects

# 上传文件临时存储目录
UPLOAD_FOLDER=./uploads

# ==================== 文件扩展名配置 ====================
# 允许的压缩包扩展名（用逗号分隔，不包含点）
ALLOWED_TAR_EXTENSIONS=tar,tar.gz
ALLOWED_ZIP_EXTENSIONS=zip

# ==================== 文件大小限制 ====================
# ZIP 文件最大大小限制（字节），默认 500MB
# 计算公式：500 * 1024 * 1024 = 524288000
ZIP_MAX_SIZE=1524288000
TAR_MAX_SIZE=2524288000

# ==================== 参赛方（Participant）评测配置 ====================
# 参赛方容器运行超时时间（秒），默认 5 分钟（300 秒）
# 超过此时间将强制停止容器并标记为超时
PARTICIPANT_TIMEOUT=300

# 参赛方容器 CPU 核心数
# Docker nano_cpus 计算公式：CPU_CORES * 1_000_000_000
# 例如：2 核心 = 2,000,000,000 nano_cpus
PARTICIPANT_CPU_CORES=2

# 参赛方容器内存限制
# 支持的单位：b, k, m, g（字节、千字节、兆字节、吉字节）
PARTICIPANT_MEM_LIMIT=8g

# ==================== 主办方（Organizer）评测配置 ====================
# 主办方容器运行超时时间（秒），默认 5 分钟（300 秒）
# 主办方通常用于评估参赛者的输出结果
ORGANIZER_TIMEOUT=1300

# 主办方容器 CPU 核心数
# Docker nano_cpus 计算公式：CPU_CORES * 1_000_000_000
ORGANIZER_CPU_CORES=2

# 主办方容器内存限制
# 支持的单位：b, k, m, g（字节、千字节、兆字节、吉字节）
ORGANIZER_MEM_LIMIT=2g
